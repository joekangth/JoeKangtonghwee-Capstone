# Finance Dashboard (React + Vite)

A simple finance dashboard where users can add stock purchases and see the current price + profit/loss.

## How to run locally
1. Install dependencies:
   ```bash
   npm install
